FILE=TABLE
PRODUCT=mrb

Group:
  Flavor=ANY
  Qualifiers=""

Common:
  Action=Setup
    setupenv()
    proddir()
    # add the bin directory to the path
    pathPrepend(PATH, ${UPS_PROD_DIR}/bin )
    setupRequired(cetpkgsupport)

END:
